# 放在这些部分下的页面将在首页和归档页面显示
mainSections = ["post"]
# 在RSS中输出页面的完整内容
rssFullContent = true
favicon        = "img/favicon.png"

# 可接受的值: "default", "lastmod"
# default = 参见 https://gohugo.io/quick-reference/glossary/#default-sort-order
# lastmod = 按最后修改日期排序，降序
SortBy              = "default"

[footer]
    since      = 2020
    customText = ""

[dateFormat]
    published   = ":date_full"
    lastUpdated = ":date_full"

[sidebar]
#    emoji    = "🍥"
    subtitle = "Lorem ipsum dolor sit amet, consectetur adipiscing elit."
    avatar   = "img/avatar.png"

[article]
    headingAnchor = false
    math          = false
    readingTime   = true

    [article.license]
        enabled = true
        default = "Licensed under CC BY-NC-SA 4.0"

[widgets]
    homepage = [
        { type = "search" },
        { type = "archives", params = { limit = 5 } },
        { type = "categories", params = { limit = 10 } },
        { type = "tag-cloud", params = { limit = 10 } },
    ]
    page = [{ type = "toc" }]

[opengraph.twitter]
    site = ""
    card = "summary_large_image"

[colorScheme]
    toggle  = true
    default = "auto"

## 评论
[comments]
    enabled  = true
    provider = "disqus"

    # 查看所有可用配置: https://github.com/CaiJimmy/hugo-theme-stack/blob/master/config/_default/params.toml
    # 从那里复制你需要的配置并粘贴到这里。
